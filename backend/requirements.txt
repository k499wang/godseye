# Backend dependency baseline from TECH_STACK.md
# Using lower-bound pins to match the planned stack while staying installable.
# Lava is intentionally omitted until the exact Python package name is confirmed.

fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
greenlet>=3.1
pydantic>=2.10
asyncpg>=0.30
httpx>=0.28
python-dotenv>=1.0
litellm>=1.55
